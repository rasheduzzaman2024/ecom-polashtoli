# Application Configuration
spring.application.name=polashtoli-store
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/polashtoli_store
spring.datasource.username=polashtoli
spring.datasource.password=Pol@shtoli.2026#
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Logging
logging.level.com.polashtoli.store=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG

# File Upload
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Jackson Configuration
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=Asia/Dhaka

# Security
jwt.secret=YourSecretKeyForJWTTokenGenerationMustBeLongEnough
jwt.expiration=86400000

# CORS
cors.allowed-origins=http://localhost:3000,http://localhost:8081
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*

# Email Configuration (Gmail example)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=2511254038@student.presidency.edu.bd
spring.mail.password=R@PU.2025#
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Payment Gateway (bKash example)
bkash.app-key=your-app-key
bkash.app-secret=your-app-secret
bkash.username=your-username
bkash.password=your-password
bkash.grant-token-url=https://tokenized.sandbox.bka.sh/v1.2.0-beta/tokenized/checkout/token/grant
bkash.create-payment-url=https://tokenized.sandbox.bka.sh/v1.2.0-beta/tokenized/checkout/create
bkash.execute-payment-url=https://tokenized.sandbox.bka.sh/v1.2.0-beta/tokenized/checkout/execute

# Chatbot Configuration
chatbot.api.url=http://localhost:5000/api
